<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.subees.submanager.subscription.model.mapper.SubscriptionCommonMapper">

    <select id="selectAllCategories"
            resultType="com.subees.submanager.subscription.model.dto.common.CategoryResponse">
        SELECT
            category_id AS categoryId,
            category_name AS categoryName
        FROM subscription_category
        ORDER BY category_id
    </select>

    <select id="selectItemsByCategoryId"
            parameterType="long"
            resultType="com.subees.submanager.subscription.model.dto.common.ItemResponse">
        SELECT
            item_id AS itemId,
            item_name AS itemName
        FROM subscription_item
        WHERE category_id = #{categoryId}
        ORDER BY item_id
    </select>

    <select id="existsCategoryById" parameterType="long" resultType="int">
        SELECT COUNT(*)
        FROM subscription_category
        WHERE category_id = #{categoryId}
    </select>

</mapper>